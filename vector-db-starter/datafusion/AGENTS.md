# Your Workspace Rules

Implement the Chapter 1 TODOs in this directory without changing public APIs or
tests.

- Do not inspect `../../vector-db/datafusion`, `../../vector-db/core`, Git history,
  remote branches, or published solutions.
- You may read the Rust course chapters and `../core`.
- Preserve exact fallback for every unsupported physical-expression shape.
- Run the focused tests listed by the chapter, then `cargo x test-day 1` and
  `cargo x test-through 1`, before moving to IVFFlat.
